# 💻 PORTAFOLIO DE PROYECTOS: PROGRAMACIÓN PROCEDURAL (C/C++)
=====================================================
             # PROGAMACIÓN PROCEDURAL
=====================================================

DESCRIPCIÓN DEL PROYECTO
-----------------------------------------------------
Este repositorio contiene la colección de prácticas, ejercicios y trabajos prácticos (T.P.) realizados para la cátedra de Programación Procedural (o Programación I, dependiendo del plan de estudios). El objetivo es almacenar y documentar el avance y la solución a los problemas propuestos en la materia, principalmente utilizando los lenguajes C y C++.

El proyecto consiste en la práctica para la cátedra de programación procedural, con trabajos realizados en C.

TECNOLOGÍAS UTILIZADAS
-----------------------------------------------------
* **Lenguajes Principales:** C y C++ (C++ se usa frecuentemente en entornos de la cátedra para problemas que inician en C, debido a la compatibilidad y herramientas disponibles).
* **Archivos de Datos:** Algunos ejercicios pueden requerir y generar archivos de datos (`.dat`) para la persistencia de la información (Ej: `jornada.dat`).
* **Compilador:** Se asume el uso de un compilador estándar como GCC/G++ o el provisto por un IDE común (como Dev-C++ o CodeBlocks).

ESTRUCTURA DEL REPOSITORIO
-----------------------------------------------------
Los ejercicios se organizan generalmente por unidades (U) y número de problema (P), aunque también pueden incluir nombres descriptivos o de autor.

* **/C++:** Contiene archivos fuente de C++ (e.g., `FACUNDO CORIA.cpp`).
* **/PROG PROCEDURAL:** Carpeta principal que alberga la mayoría de las prácticas.
    * **Archivos de Práctica:** Nombrados bajo el formato **P[#]U[#]** (Problema X, Unidad Y).
        * Ejemplos: `P1U2.cpp`, `P5U3.cpp`, `p7u5.cpp`, `P11U6.cpp`.
    * **Trabajos Especiales/Parciales:** Incluye ejercicios de evaluación o recuperación.
        * Ejemplos: `PUNTO1PARCIAL1.cpp`, `recuperacion_facundo_coria.c`.
    * **Ejecutables:** Archivos compilados generados (e.g., `.exe`), como `p6u4.exe`.
    * **Archivos Adicionales:** Puede haber archivos de datos (`.dat`) o imágenes (`.jpg`) relacionados con los problemas.

CÓMO COMPILAR Y EJECUTAR
-----------------------------------------------------
Para compilar y ejecutar cualquiera de los archivos fuente (`.c` o `.cpp`):

1.  **Instalar un Compilador:** Asegúrate de tener GCC/G++ instalado en tu sistema.
2.  **Navegar al Directorio:** Abre una terminal o Símbolo del Sistema y navega hasta la carpeta que contiene el archivo fuente (e.g., `/PROG PROCEDURAL`).
3.  **Compilar (para C++):**
    ```
    g++ -o nombre_del_ejecutable nombre_del_archivo.cpp
    ```
    *Ejemplo:* `g++ -o p5u3 p5u3.cpp`
4.  **Compilar (para C):**
    ```
    gcc -o nombre_del_ejecutable nombre_del_archivo.c
    ```
    *Ejemplo:* `gcc -o po2 "FACUNDO CORIA PO2.c"`
5.  **Ejecutar:**
    ```
    ./nombre_del_ejecutable
    ```
    *Ejemplo:* `./p5u3`

AUTOR / CONTACTO
-----------------------------------------------------
Para consultas sobre los ejercicios o el contenido de este repositorio.
facundocoriahaddad@gmail.com
https://www.linkedin.com/in/facundogabrielcoriahaddad/

* **Autor:** Facundo Coria
